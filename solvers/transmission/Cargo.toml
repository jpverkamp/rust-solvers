[package]
name = "transmission"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
solver = { version = "0.1.0", path = "../../libs/solver" }
tracing = "0.1.41"
